---
description: Spawn a Claude worker in a new kitty window for a task
argument-hint: "<task>"
---
Spawn a Claude worker using the delegate.sh script. Run:

```bash
~/.pi/agent/skills/claude-delegate/delegate.sh --prompt "$ARGUMENTS" --title "delegate" --save ~/claude-output.md
```

Do it now — do not just show the command, actually execute it.
